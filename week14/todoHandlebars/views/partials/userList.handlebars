<ul>
    {{#each users as |user|}}
    <li>
        <p>User ID: {{user.id}}</p>
        <p>Username: {{toUpperCase user.username}}</p>
        <p>Email: {{user.email}}</p>
    </li>
    {{/each}}
</ul>